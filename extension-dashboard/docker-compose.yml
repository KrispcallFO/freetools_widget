version: '3.8'

services:
  backend:
    build:
      context: .                         # ✅ change to root
      dockerfile: ./Server/Dockerfile   # ✅ still use server’s Dockerfile
    container_name: backend
    ports:
      - "3000:3000"
    env_file:
      - ./Server/.env
    environment:
      - DATABASE_URL=postgresql://towniz:secret123@212.38.95.16:5433/townizdb
    restart: unless-stopped
